// We need a function that can transform a number (integer) into a string.

// This function converts a number into a string by using the toString method
// At first I was thinking about what to use and I realized that I could make this simple by using the method.
function numberToString(num) {
  return num.toString();
}